{% if is_granted('ROLE_PREVIOUS_ADMIN') %}
    <h2 class="w-100 text-center mx-auto d-block">
        <a href="{{ path('lift_homepage', {'_switch_user': '_exit'}) }}">
            End Impersonation
        </a>
    </h2>
{% endif %}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="">
        <img class="nav-profile-img rounded-circle" src="{{ app.user.avatarUrl(100) }}">
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('lift_account') }}">Account</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="dropdownFuryExercise" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Exercises
                </a>
                <div class="dropdown-menu" aria-labelledby="dropdownFuryExercise">
                    <a class="dropdown-item" href="">All Exercises</a>
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class="dropdown-item" href="">Create Exercise</a>
                    {% endif %}
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="dropdownFuryExerciseCategory" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Categories
                </a>
                <div class="dropdown-menu" aria-labelledby="dropdownFuryExerciseCategory">
                    <a class="dropdown-item" href="">All Categories</a>
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class="dropdown-item" href="">Create Category</a>
                    {% endif %}
                </div>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('lift_logout') }}">Logout</a>
            </li>
        </ul>
        <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
        </form>
    </div>
</nav>